ojdbc6를 다운받을 때
밑에 다른 주소에서 제공하는 라이브러리는 다운받을 수 없다.

마이바티스 질의명령 xml파일로 관리

설정 !DODTYPE configuration

질의명령 mapper태그

빈처리 = new 시키는 것

프로젝트 web.xml에 인코딩 설정하는 이유
 서버에 여러개의 프로젝트가 올라갈 수 있으니 각각 세팅할 수 있도록
 
 리다이렉트는 겟방식 파라미터를  넘겨야함
 
 파라미터를 노출이 안되도록 하고 싶으면 post방식
 포워드로 리다이렉트 페이지를 만든다.
 거기서 폼태그에서  post 방식으로 전송
 
 파라미터는 리퀘스트 객체안에 포함되어있다.
 
 name 속성 : 파라미터의 키값으로 쓰임
 
 클래스타입(객체타입)은 null세팅 가능
 기본데이터 타입은 null불가 0 으로 가능
 
 datasource: 커넥션 풀 관리하는 클래스
 
 tx/context 라이브러리 처리 servlet(aop), pom.xml, root-context
 
 log4j : 로그처리 설정파일
 
 radio 속성은 네임값이 같은 것끼리중에 하나만 선택할 수 있다.
 